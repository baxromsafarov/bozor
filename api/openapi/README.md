# OpenAPI-контракты

Спецификации OpenAPI 3.1 внешнего REST API, по файлу на сервис: `<service>.yaml`.

Наполняются по мере реализации соответствующих Stage (см. `docs/ROADMAP.md`).
Конвенции API — в `docs/RULES.md` (раздел «Версионирование API») и `docs/ARCHITECTURE.md`.
