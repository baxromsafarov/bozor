# Proto-контракты (gRPC)

Protobuf-описания внутренних gRPC API, по каталогу на сервис: `<service>/`.
Линт и генерация — через buf (см. `buf.yaml` в корне, появится на Stage 0.2).

Наполняются по мере реализации соответствующих Stage (см. `docs/ROADMAP.md`).
